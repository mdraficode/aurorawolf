.ghtoken
node_modules/
__pycache__/

# regenerable screenshots (test suites & probes write these — never commit)
shots/*.png
shots/*.jpg
shots/*.jpeg
shots/*.webp

# transient spawn output (regenerated by 'spawn')
training/rafzzer_candidate.json

# probe run reports & their screenshots (regenerable by test/speedrun/*.mjs)
test/speedrun/runs/
uploads/

# big downloaded backups (relay/ was retired once its payload was reconciled into
# the repo; the env-relay workflow + helper files were removed with it)
