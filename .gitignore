.ghtoken
node_modules/
__pycache__/

# regenerable screenshots (test suites & probes write these)
shots/*.png

# transient spawn output (regenerated by 'spawn')
training/rafzzer_candidate.json

# probe run reports & their screenshots (regenerable by test/speedrun/*.mjs)
test/speedrun/runs/
uploads/

# env-relay payload (the 28 MB Drive/WeTransfer archive) is a big binary — keep it
# out of git; the audit trail (MANIFEST.tsv + NESTED_REPO_STATE.txt) IS committed.
drive_drop/
