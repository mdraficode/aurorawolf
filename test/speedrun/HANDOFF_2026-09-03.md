# Handoff — 2026-09-03 (human-speedrun session, continued)

**HEAD `482c1a0`; dirty (uncommitted, intended):** `test/speedrun/human.mjs` (warm-up gate),
`test/speedrun/probe_fight.mjs` (v11→v25 ladder, RE default reverted to 30),
`test/speedrun/run.mjs` (fight grammar ports, iron route v3, top-up/hygiene fixes).

## What was settled this session

- **The fight grammar ladder is finished: PARK (v25) is the law.** Probe v25 measured
  8 hits (0.22/s), 0.87 dps, kill 52 s, net −18 of 196 hp, stamina floor 113 — the only
  positive result against the real fight geometry. The band ring (the probe's BASE law)
  kills in 39–56 s at net −37..−48 — wins, but bleeds. Everything else (v11–v24) loses.
  **Variance remains:** the same v25 code rolled 0.50 dps / 5.41 incoming on a second run
  (phase rolls). The park is proven, not yet robust — see the open item below.
- **The probe's "NET LOSING" verdict is a strawman** (ignores the hp budget). Real test:
  `kill_time × (incoming − regen) < wolfHp`.
- **The real run's failure chain (8 attempts, 0 bites) was the rig's, not the fight's:**
  (1) top-up after the channel check = dead code → the wolf arrived every fight at
  stam 5–16; now FIXED (top-up rest at stam < 80 / hp < 90% *before* the `d < 3.2` channel,
  predator-aware, 40-poll timebox — the ritual marker now logs `stam:99, hp:140`).
  (2) the doomed flee (2,700 polls) — now floored at stam ≥ 15 (below it: stand and die —
  the game's intended retry, the respawn is a full tank). (3) the shut-in entry needs
  stamina (was walking into a 12.5 m/s pursuer). (4) default now `--fighttac=park`.
- **BUGS.md** written (G-1..G-6 game-side confirmations, R-1..R-7 rig-side fixes, O-1..O-2
  open). **TRAINING_MANUAL.md** updated (§4 verdict + ranking, §5.2 park row, §5.3
  v18–v25 + real-fight rows, §5.4 the honest number, §5.5 drills 7–9: the PARK, the
  ARRIVAL LAW, the strawman metric; §7 session pointer).

## The one open item (next session, first task)

**The park does not engage in the real run's integration.** `runs/iron_park2.log`: after the
top-up fix the wolf enters at stam 77–99, but the fm path goes −0.8 → +0.12 (wraps through
the tail); the arrive-sprint drains 77 → 11 in ~5 sim-s; the park gates never hold. Suspects,
in order: (a) the park zone gate (`fm < −1.5`, or `−0.9` only at stam ≤ 14) is too strict —
widen to engage at `fm < −0.9` with the walk last-stretch from `fm < −0.6`; (b) the arrive
cut 1.50 overshoots — 1.45; (c) `b.turn` may be undefined in the real eyes → `rPark` falls
back to 7/2.53 = 2.77 — confirm via a `mark('fight', {turn: b.turn})`; (d) check whether the
knockback re-loosens the park and needs a re-arrive.

Repro: `node test/speedrun/run.mjs --route=iron --seed=7777 --cap=3600 --speed=8 --rate=3
--re=10` (log `runs/iron_park2.log` is the current record of the fixed top-up + park).

## Next milestones

1. Park engagement fix → first real boss kill (the `boss-end {res:'slain'}` and trophy).
2. Then rank rush / pack / hunt against the park (all four routes now share the same
   fight grammar; iron at natural L5 is the shortest measured line).
3. Bot-training gate: LAW v4 still stands — the human/trainer verdict on the fight comes
   from THIS ladder; the generations should be coached to the park grammar (drill 7).
